import {useRouter} from "next/router";

export const SignOut = () => {
    const router = useRouter();

    return (
        <div>
            Signing Out
        </div>
    )
}

export default SignOut;
